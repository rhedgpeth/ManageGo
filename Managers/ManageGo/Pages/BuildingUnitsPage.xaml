<?xml version="1.0" encoding="UTF-8"?>
<local:BaseSearchContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ManageGo.UI.Pages"
    xmlns:vm="clr-namespace:ManageGo.Core.Managers.ViewModels"
    x:TypeArguments="vm:BuildingUnitsViewModel"
    x:Class="ManageGo.Pages.BuildingUnitsPage"
    ControlTemplate="{StaticResource HeaderPageTemplate}">
    <ContentPage.Content>

    <StackLayout>
        <Grid RowSpacing="15" Padding="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="1" />
            </Grid.RowDefinitions>

            <StackLayout Grid.Row="0" Grid.Column="1" VerticalOptions="Center">
                <Image Source="building_grey" Aspect="AspectFit" />
            </StackLayout>

            <Label Grid.Row="0" Grid.Column="2" Text="{Binding BuildingName}" FontSize="16" FontAttributes="Bold" Margin="10,0,0,0" />

            <BoxView Grid.Row="1" Grid.ColumnSpan="3" HeightRequest="1" HorizontalOptions="FillAndExpand" BackgroundColor="#DFDFE0" />
        </Grid> 

    </StackLayout>
        
    </ContentPage.Content>
</local:BaseSearchContentPage>
